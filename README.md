# RAG_Project
End-to-end RAG pipeline on 10K+ documents achieving ~85% answer faithfulness. Semantic Redis caching cut LLM response time from ~3s to under 1s (~38% reduction in redundant API calls). Eval harness measured faithfulness, citation accuracy, and retrieval precision.
